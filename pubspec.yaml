name: country_list_picker
description: CountryListPicker for Flutter customizable country selection package with multi-languages support.
version: 1.0.1
homepage: https://github.com/AmremadDev/country-list-picker.git
# publish_to: none

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^2.0.1
  mask_text_input_formatter: ^2.4.0
  provider: ^6.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/flags/
 