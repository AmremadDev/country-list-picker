class SearchTileTheme {
  final bool visible;
  final String title;
  final String hint;
  const SearchTileTheme({
    this.visible = true,
    this.title = "Search",
    this.hint = "name/dial code...",
  });
}
